---
artifact_type: pre_computation_block
task_id: d583385-prettierignore
timestamp: 2026-07-08T06:28:26Z
complexity_score: 4
complexity_tier: STANDARD
---

## Complexity Score

| Dimension    | Value | Rationale                                                                                     |
|--------------|-------|----------------------------------------------------------------------------------------------|
| Scope Size   | 2     | > 3 files: a new `.prettierignore` + 11 reformatted source/doc files.                        |
| Ambiguity    | 1     | The ignore scope (vendored framework dirs, not just the records the issue named) + the docs-format decision needed a call (user chose: format the 4 docs). |
| Risk Surface | 1     | `.prettierignore` changes what `npm run format` touches for everyone; the pass edits CLAUDE.md/README/CONTEXT (project instructions + runbook). Pure formatting, no logic. |
| Knowledge Gap| 0     | `prettier --check .` gives the exact drift set; categorisation is mechanical.                 |
| **Sum**      |       | **4 → STANDARD**                                                                              |

## Assumptions (ranked)

| # | Assumption | Confidence | Verification |
|---|------------|------------|--------------|
| 1 | `.artifacts/`, `SESSION_LOG.md`, `METRICS.md` are append-only / tool-generated records; formatting them corrupts the historical trail. | HIGH | Issue body says exactly this; METRICS.md is regenerated by `aggregate-metrics.sh`; SESSION_LOG is append-only per templates/session_journal.md. |
| 2 | `skills/`, `examples/`, `templates/`, `schemas/`, `tools/` are the **vendored** `@YackShavingSkill` framework — reformatting forks them from upstream, same rationale as the records. | HIGH | CLAUDE.md: "vendored locally: `skills/ examples/ templates/ tools/ schemas/`". |
| 3 | `.serena/project.yml` is Serena-MCP-managed config, not our source. | HIGH | `.serena/` is the Serena tool's project dir. |
| 4 | The 11 remaining drifted files are all our own source/docs and safe to `prettier --write`. | HIGH | `prettier --check .` list minus the ignored set = config.js, recap.js, test-token.js, test/{modules,recap}.test.js, evals/{run-recap-eval.js,README.md}, README.md, CONTEXT.md, CLAUDE.md, hermes-discord-bot.md. |
| 5 | Formatting is behaviour-preserving — `npm test` stays 86/86 and `eslint .` stays 0 after the pass. | HIGH | Prettier only restyles whitespace/quotes/tables; no token semantics change. Verified post-hoc. |
| 6 | Prettier does not process `waysofworking.org` (no org parser), so it needs no ignore entry. | MEDIUM | It is absent from the `prettier --check .` drift list. |

## Plan

1. Add `.prettierignore` with two grouped sections (append-only records; vendored framework + tool config).
2. `prettier --write` exactly the 11 our-source/doc files (list them explicitly — do NOT run `prettier --write .`, which would touch the now-ignored set only after the ignore lands; explicit list is safe either way and documents intent).
3. Verify `npx prettier --check .` is clean, `npm test` 86/86, `eslint .` 0.
